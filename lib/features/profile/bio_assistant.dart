import 'package:flutter/material.dart';

class BioAssistant extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Card(
      child: Padding(
        padding: const EdgeInsets.all(16.0),
        child: Column(
          children: [
            Text('AI Bio Assistant'),
            // Add AI assistant UI here
          ],
        ),
      ),
    );
  }
}
