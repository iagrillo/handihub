import 'package:flutter/material.dart';
import '../../profile/submit_profile.dart' as main_profile;

class SubmitProfile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Reuse the main SubmitProfile widget for consistency
    return main_profile.SubmitProfile();
  }
}
