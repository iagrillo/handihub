import 'package:flutter/material.dart';

class DirectoryHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Directory Home')),
      body: Center(child: Text('Welcome to the Directory!')),
    );
  }
}
